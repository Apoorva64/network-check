{
  "name": "network-check",
  "version": "0.0.0",
  "description": "Simple application designed to check network connectivity within a Kubernetes cluster and report the information to Prometheus for monitoring purposes.",
  "private": true,
  "scripts": {
    "semantic-release": "semantic-release"
  },
  "devDependencies": {
    "@codedependant/semantic-release-docker": "^5.0.0",
    "@semantic-release/changelog": "^6.0.3",
    "@semantic-release/git": "^10.0.1",
    "semantic-release": "latest"
  },
  "dependencies": {
    "@semantic-release/commit-analyzer": "^13.0.1",
    "@semantic-release/github": "^11.0.3",
    "@semantic-release/release-notes-generator": "^14.0.3"
  }
}
